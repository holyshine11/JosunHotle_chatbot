# ============================================================
# 환경변수 설정 (.env)
# 이 파일을 .env로 복사 후 값 수정
# ============================================================

# LLM 설정 (Groq API 사용 - 서버에 Ollama 없음)
USE_GROQ=true
GROQ_API_KEY=gsk_여기에_발급받은_키_입력

# Groq 모델 (기본: llama-3.1-8b-instant)
# 한국어 품질을 위해 llama-3.3-70b-versatile 권장 (무료 tier 내)
# GROQ_MODEL=llama-3.3-70b-versatile

# 서버 설정
PORT=8000
ALLOWED_ORIGINS=*

# 캐시 설정
LLM_CACHE_ENABLED=true
LLM_CACHE_SIZE=100
